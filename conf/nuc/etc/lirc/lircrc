
begin
button = KEY_UP
prog   = irexec
config = xdotool key Up 
end

begin
button = KEY_DOWN
prog   = irexec
config = xdotool key Down
end

begin
button = KEY_RIGHT
prog   = irexec
config = xdotool key Right
end

begin
button = KEY_LEFT
prog   = irexec
config = xdotool key Left
end

begin
button = KEY_SELECT
prog   = irexec
config = xdotool key Return
end

begin
button = KEY_BACK
prog   = irexec
config = xdotool key BackSpace
end

begin
button = KEY_PLAY
prog   = irexec
config = xdotool key space
end

begin
button = KEY_STOP
prog   = irexec
config = xdotool key x
end

begin
button = KEY_FASTFORWARD
prog   = irexec
config = xdotool key f
end

begin
button = KEY_REWIND
prog   = irexec
config = xdotool key r
end

begin
button = KEY_DVD
prog   = irexec
config = /home/jenia/my/conf/nuc/run-show-kodi
end

