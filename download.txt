#!/bin/bash

Dir=$(dirname $0)
echo Updating files in $Dir from goo.gl/zvmBBz. Please, wait...
cd $Dir

Log=$(mktemp)

(
curl -# -L goo.gl/zvmBBz -o file.zip && unzip -o file.zip ; rm -f file.zip ; cd -
) &> $Log || cat $Log

rm $Log
